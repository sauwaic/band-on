<div class="container">
  <div class="row">
    <div class="col-xs-12">

        <h1>Step 2: Select slot</h1>


      <% @available_slots.each do |slot| %>
      <p> <%= link_to "#{slot.date}", new_group_path(slot_id: slot.id) %> </p>

      <% end %>

    </div>
  </div>
</div>
