<div class="container">
  <div class="row">
    <div class="col-xs-12">

    <h1>Select jam session:</h1>
      <% @groups.each do |group| %>
      <%= render 'group_card', group: group %>
      <% end %>

    </div>
  </div>
</div>
