<div class="container">
  <div class="row">
    <div class="col-xs-12">

    <h3> <%= @group.name %> </h3>
      <p> <%= "Group admin: #{@group.admin.name}" %> </p>
      <p> <%= "Group description: #{@group.description}" %> </p>
      <p> Group members:
      <% @group.users.each do |member| %>
        <%= "#{member.name} |" %>
        <% end %>
      </p>

    </div>
  </div>
</div>
